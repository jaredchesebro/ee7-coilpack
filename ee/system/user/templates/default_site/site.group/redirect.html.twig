{% set entry = exp.channel.entries({channel:"redirects",dynamic:true,limit:1}).first() %}

{% set redirect = '404' %}
{% if entry.redirect_url|length %}
    {% set redirect = entry.redirect_url %}
{% endif %}

{{ exp.redirect(redirect) }}
